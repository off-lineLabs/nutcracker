package com.example.template.data.repo

// Legacy in-memory Piece-based repository was removed. All app data uses Room-backed
// FoodLogRepository in this package. This empty file remains only to avoid references
// from older imports and can be deleted once all usages are gone.
